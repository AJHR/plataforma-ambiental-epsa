# @repo/content — Contenido placeholder y plantillas

Contenido **de ejemplo** (no definitivo) que EPSA reemplazará en fase posterior. Todo debe quedar marcado como placeholder. Se genera en WS9 (ver `docs/MODULOS_Y_CONTENIDO.md`).

```
templates/   # plantillas Excel/CSV de carga por componente (aire, ruido, vibraciones, fauna, biota)
seed/        # JSON de ejemplo: home, proyecto, faq, glosario, cápsulas educativas, boletines, noticias, casos MIAQR
geo/         # GeoJSON de las 4 áreas + puntos de monitoreo de ejemplo
docs/        # PDFs placeholder (marca de agua "DOCUMENTO DE EJEMPLO")
```
